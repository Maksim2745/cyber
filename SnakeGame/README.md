# SnakeGame (C# Console)

Простая игра «Змейка» на C# для консоли.

## Требования

- .NET SDK 8.0+

## Запуск

```bash
dotnet run --project SnakeGame/SnakeGame.csproj
```

## Управление

- `WASD` или стрелки — движение
- `Q` или `Esc` — выход
- после окончания игры: `R`/`Enter` — начать заново
